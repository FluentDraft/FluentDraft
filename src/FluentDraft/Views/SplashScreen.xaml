<Window x:Class="FluentDraft.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FluentDraft" Height="165" Width="480"
        WindowStyle="None" ResizeMode="NoResize" 
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True" ShowInTaskbar="False">

    <Border CornerRadius="12">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#CC1E1E1E" Offset="0"/>
                <GradientStop Color="#EE121212" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid Margin="20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Logo area -->
            <Image Grid.Column="0" Source="/FluentDraft;component/Icons/app_icon.png" 
                   Width="64" Height="64" 
                   VerticalAlignment="Center"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Margin="10,0,25,0"/>
            
            <!-- Text Area -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <!-- App Title with Shimmer -->
                <TextBlock Text="FluentDraft" 
                           FontSize="28" FontWeight="Bold" FontFamily="Segoe UI Variable Display, Segoe UI"
                           HorizontalAlignment="Left"
                           Margin="0,0,0,5">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                            <GradientStop Color="#99FFFFFF" Offset="0" x:Name="GradientStop1"/>
                            <GradientStop Color="White" Offset="0.5" x:Name="GradientStop2"/>
                            <GradientStop Color="#99FFFFFF" Offset="1" x:Name="GradientStop3"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="GradientStop1" Storyboard.TargetProperty="Offset" From="-1" To="2" Duration="0:0:3"/>
                                    <DoubleAnimation Storyboard.TargetName="GradientStop2" Storyboard.TargetProperty="Offset" From="-0.5" To="2.5" Duration="0:0:3"/>
                                    <DoubleAnimation Storyboard.TargetName="GradientStop3" Storyboard.TargetProperty="Offset" From="0" To="3" Duration="0:0:3"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>

                <!-- Slogan -->
                <TextBlock Text="Your thoughts, refined."
                           Foreground="#AAAAAA"
                           FontSize="14"
                           FontFamily="Segoe UI"
                           HorizontalAlignment="Left"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
